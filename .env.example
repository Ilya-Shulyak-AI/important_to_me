# Important to Me does not require environment variables for local development.
# Set DISABLE_HMR=true only when running in an environment where Vite file watching
# or hot module replacement causes excessive CPU usage.
DISABLE_HMR=false
